# App data and backups (kept outside git)
data/intake.sqlite
data/intake.sqlite-journal
data/intake.sqlite-wal
data/intake.sqlite-shm
data/intake.sqlite.bak-*
data/backups/
data/chunks/
data/sku_photos/
data/ebay_images/
data/layouts/

# Logs
logs/lookup.csv
logs/archive/

# Alerts config (contains secrets if created)
scripts/alert.config.ps1

# Local app secrets
.env
.aider*

# Tooling / scratch
.freebuff/
desktop-v2.db

# Test sandbox databases
tmp/test_sandbox_data/

# Runtime state files (auto-generated)
data/.asset_version
data/.storage_ok

# Live archive database (rebuilt by scripts/build_archive_db.php)
data/archive.sqlite

# Logs (runtime artifacts)
logs/*.log

# PHPUnit cache
.phpunit.cache/

# Alert email password (DPAPI-encrypted secret)
scripts/alert.password.txt
